---
phase: 02-fejlrettelser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aula/timezone_utils.py
  - outlookmanager.py
  - aula/aula_calendar.py
  - main.pyw
  - setupmanager.py
  - tests/phase02/test_dst_helpers.py
  - tests/phase02/test_aula_time_windows.py
autonomous: true
requirements:
  - BUG-01
  - BUG-02
must_haves:
  truths:
    - "Outlook-begivenheder i juli 2026 og andre fremtidige sommertidsperioder faar +02:00 i Aula uden hardkodet aarstal-tabel"
    - "Aula-opslag bruger dynamiske Copenhagen-offsets per lookup-vindue i stedet for et enkelt is_in_daylight-flag for hele syncen"
    - "Aula start- og sluttidsstrenge er normaliseret, saa DST-graenser ikke giver falske opslag eller forkert offset-format"
    - "setupmanager.py kan importeres paa Python 3.12 og Python 3.13 uden distutils-fejl"
  artifacts:
    - path: "aula/timezone_utils.py"
      provides: "Faelles helper til Copenhagen-offset og Aula-datetime-formattering"
      min_lines: 20
    - path: "tests/phase02/test_dst_helpers.py"
      provides: "Bevis for korrekte DST-graenser i 2026 og frem"
      min_lines: 20
    - path: "tests/phase02/test_aula_time_windows.py"
      provides: "App-naer verifikation af Aula lookup-vinduer over DST-graenser"
      min_lines: 20
  key_links:
    - from: "outlookmanager.py"
      to: "aula/timezone_utils.py"
      via: "Outlook-konvertering genbruger samme offset-logik som Aula-opslag"
      pattern: "Europe/Copenhagen|\\+02:00|\\+01:00"
    - from: "aula/aula_calendar.py"
      to: "tests/phase02/test_aula_time_windows.py"
      via: "DST-graensecases i tests matcher den formattering getEvents bygger"
      pattern: "2026-03-29|2026-10-25"
---

<objective>
Ret Phase 02 planens tidskritiske tidszonefejl og Python 3.12+-startblokering.

Purpose: BUG-01 er ikke kun den hardkodede sommertidstabel i `outlookmanager.py`; den aktive runtime bruger ogsaa et enkelt daylight-flag i `AulaCalendar.getEvents()`, som giver forkerte Aula-opslag naar sync-vinduet krydser DST-graenser. BUG-02 er en import-time blocker, der skal fjernes tidligt, fordi den ellers undergraver hele Phase 02-verifikationen.

Output: En delt Copenhagen-timezone-helper, opdaterede Outlook/Aula-kodeveje, fjernet `distutils`-import og fokuserede `tests/phase02`-checks for DST og Aula-tidsvinduer.
</objective>

<execution_context>
@C:/Users/Jesper/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Jesper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-fejlrettelser/02-CONTEXT.md
@.planning/phases/02-fejlrettelser/02-RESEARCH.md
@.planning/phases/02-fejlrettelser/02-VALIDATION.md
@main.pyw
@outlookmanager.py
@setupmanager.py
@aula/aula_calendar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Indfoer delt Copenhagen-timezone-helper og brug den i Outlook-konverteringen</name>
  <files>aula/timezone_utils.py, outlookmanager.py</files>
  <action>
Opret `aula/timezone_utils.py` som et lille, fase-specifikt helpermodul med stdlib `zoneinfo` og `Europe/Copenhagen`. Implementer mindst to helpers:

- en helper der returnerer Aula-offset som `+01:00` eller `+02:00` for en given lokal datetime
- en helper der bygger komplette Aula-datetime-strenge fra en lokal datetime uden at haardkode aarstal

Opdater derefter `OutlookManager` til at bruge denne helper i stedet for den nuvaerende `is_in_daylight()`-tabel for 2021-2025. Fjern eller omskriv `is_in_daylight()` saa dens adfaerd er dynamisk og ikke laengere bygger paa haardkodede perioder. Bevar nuvaerende adfaerd for heldagsbegivenheder og kategori-filtrering; scope er kun tidskorrekthed.

Hvis du udtraekker en formatteringshelper, saa brug den til baade start- og sluttid, saa Outlook-konverteringen ikke har to forskellige offset-kilder.
  </action>
  <verify>
    <automated>py -3 -m pytest tests/phase02/test_dst_helpers.py -q</automated>
  </verify>
  <done>`outlookmanager.py` bruger ikke laengere en haardkodet DST-tabel; juli 2026 og de definerede 2026-graensedatoer giver korrekte Aula-offsets via en delt helper</done>
</task>

<task type="auto">
  <name>Task 2: Ret Aula lookup-vinduerne saa DST beregnes per lokal datetime</name>
  <files>aula/aula_calendar.py, main.pyw, tests/phase02/test_aula_time_windows.py</files>
  <action>
Fjern afhængigheden af `is_in_daylight`-booleans i den aktive sync-sti. Opdater `main.pyw` og `AulaCalendar.getEvents()` saa kaldet ikke laengere sender et enkelt daylight-flag for hele syncvinduet, men i stedet bruger den nye helper til at formatere `start` og `end` per lookup-vindue.

Mens du roerer koden, normaliser den nuvaerende timestamp-formattering i `AulaCalendar.getEvents()` saa den producerer valide Aula-strenge uden det nuvaerende literal-`T`-hack foran offsettet. Bevar month-by-month lookup-strategien; ændringen er korrekt offset-beregning, ikke ny hentelogik.

Opret `tests/phase02/test_aula_time_windows.py` med app-naere cases der mindst dækker:
- 2026-03-28 -> `+01:00`
- 2026-03-29 -> `+02:00`
- 2026-07-15 -> `+02:00`
- 2026-10-25 -> `+01:00`

Testene skal bevise baade helperniveau og den strengformatering `getEvents()` faktisk bruger, saa validationen ikke kun er indirekte.
  </action>
  <verify>
    <automated>py -3 -m pytest tests/phase02/test_aula_time_windows.py -q</automated>
  </verify>
  <done>`main.pyw` og `AulaCalendar.getEvents()` bruger samme dynamiske offset-kilde, og Aula-opslag over 2026-DST-graenser formateres med korrekte offsets</done>
</task>

<task type="auto">
  <name>Task 3: Fjern Python 3.12+-blokeringen og koer startup-smoke for BUG-02</name>
  <files>setupmanager.py</files>
  <action>
Fjern den ubrugte `distutils`-import fra `setupmanager.py`. Erstat den ikke med ny packaging-logik medmindre et konkret call-site faktisk mangler noget efter fjernelsen; research viser at importen er ubrugt og alene er en startup-blocker.

Koer derefter den mindste realistiske smoke-verifikation mod Python 3.12 og Python 3.13. Hvis en af interpreterne ikke findes lokalt, saa dokumenter den konkrete begrænsning i planens summary-output i stedet for at opfinde en alternativ "best guess"-verifikation.
  </action>
  <verify>
    <automated>py -3.12 -c "import setupmanager"</automated>
  </verify>
  <done>`setupmanager.py` kan importeres uden distutils-fejl, og mindst Python 3.12 er konkret smoketestet lokalt; 3.13 er enten verificeret eller eksplicit dokumenteret som miljobegrænsning</done>
</task>

</tasks>

<verification>
- `py -3 -m pytest tests/phase02/test_dst_helpers.py -q`
- `py -3 -m pytest tests/phase02/test_aula_time_windows.py -q`
- `py -3.12 -c "import setupmanager"`
- `py -3.13 -c "import setupmanager"`
- `py -3 -m py_compile main.pyw setupmanager.py outlookmanager.py aula\\aula_calendar.py aula\\timezone_utils.py`
- Dokumenter konkrete 2026-beviser i summaryen: `2026-03-28 -> +01:00`, `2026-03-29 -> +02:00`, `2026-07-15 -> +02:00`, `2026-10-25 -> +01:00`
</verification>

<success_criteria>
1. BUG-01 er lukket i baade Outlook-konverteringen og Aula-opslagene, saa 2026+ tider ikke forskydes en time.
2. BUG-02 blokerer ikke laengere startup eller import paa Python 3.12+.
3. Phase 02 har lokalt, reproducerbart bevis for DST-adfaerd i stedet for kun en kodeinspektion.
</success_criteria>

<output>
Efter afslutning, opret `.planning/phases/02-fejlrettelser/02-01-SUMMARY.md` med:
- hvad der blev aendret i timezone-logikken
- hvilke 2026-datoer der blev verificeret
- resultatet af Python 3.12/3.13 smoke-checks og eventuelle miljoebegrænsninger
</output>
