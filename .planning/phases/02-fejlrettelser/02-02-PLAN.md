---
phase: 02-fejlrettelser
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - main.pyw
  - aula/aula_calendar.py
  - version.txt
  - tests/phase02/conftest.py
  - tests/phase02/test_internet_notifications.py
  - tests/phase02/test_delete_failure_paths.py
  - tests/phase02/test_version_fallback.py
autonomous: true
requirements:
  - BUG-03
  - BUG-04
  - BUG-05
must_haves:
  truths:
    - "Manglende internetforbindelse giver GUI-logfejl i stedet for NameError-crash"
    - "Default-kanalen for internetfejl er GUI + tray, og tray-popup throttles til foerste gentagne fejl indtil en vellykket sync nulstiller state"
    - "deleteEvent-fejlstien kaster ikke NameError, og callers i main.pyw behandler False som fejl i stedet for succes"
    - "Programmet kan starte uden .git-mappe; naar version.txt findes vises datoen, og naar den mangler skjules labelen"
  artifacts:
    - path: "version.txt"
      provides: "Statisk fallback-metadata til versionsvisning uden git"
      min_lines: 1
    - path: "tests/phase02/test_internet_notifications.py"
      provides: "Bevis for GUI-log, tray-throttle og reset-efter-succes"
      min_lines: 20
    - path: "tests/phase02/test_version_fallback.py"
      provides: "No-.git startup-bevis med og uden version.txt"
      min_lines: 20
  key_links:
    - from: "main.pyw"
      to: "version.txt"
      via: "Versionslabel bruger git hvis muligt, ellers version.txt, ellers skjules labelen"
      pattern: "program_version_label|version.txt|git.Repo"
    - from: "tests/phase02/test_delete_failure_paths.py"
      to: "aula/aula_calendar.py"
      via: "False/None-semantik og delete/update-failure-paths er eksplicit verificeret"
      pattern: "deleteEvent|updateEvent"
---

<objective>
Ret Phase 02s startup- og fejlsti-resiliens for BUG-03, BUG-04 og BUG-05.

Purpose: Den synlige NameError ved manglende internet er kun det foerste symptom; research viser at `MainWindow` har logger/bootstrap-risiko tidligt i startup, at `deleteEvent()`-rettelsen ellers giver falske succeslogninger i callerne, og at `.git`-afhaengigheden skal erstattes af en reel metadata-fallback for at kunne verificeres sandfaerdigt.

Output: Central internetfejl-notifikation i `main.pyw`, rettede delete/update-success checks, no-`.git`-sikker versionsopslag med `version.txt`, og maalrettede tests/fixtures for disse paths.
</objective>

<execution_context>
@C:/Users/Jesper/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Jesper/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-fejlrettelser/02-CONTEXT.md
@.planning/phases/02-fejlrettelser/02-RESEARCH.md
@.planning/phases/02-fejlrettelser/02-VALIDATION.md
@main.pyw
@aula/aula_calendar.py
@O2A.spec
@main.spec
</context>

<tasks>

<task type="auto">
  <name>Task 1: Goer startup og internetfejl-haandtering sikre i MainWindow</name>
  <files>main.pyw, tests/phase02/conftest.py, tests/phase02/test_internet_notifications.py</files>
  <action>
Flyt eller initialiser logger-relateret state tidligt nok i `MainWindow.__init__()` til at startup-naere helpers ikke kan ramme en uinitialiseret `self.logger`. Implementer derefter en lille central helper i `main.pyw` for internetfejl-feedback med de laaste fasebeslutninger:

- GUI-log skrives altid
- default-kanal er `GUI + tray`
- tray-popup vises kun foerste gang ved gentagne internetfejl
- en vellykket sync nulstiller tray-throttle-state
- modal er ikke aktiv som default, men koden maa ikke laase fast paa kun en kanal

Brug helperen fra baade `on_runO2A_clicked()` og `on_forcerunO2A_clicked()` i stedet for de nuvaerende bare `logger.critical(...)`-kald. Hold implementeringen lokal til `main.pyw`; lav ikke en ny konfigurationsskærm i denne fase.

Opret `tests/phase02/conftest.py` med de mindste stubs/fixtures der er noedvendige for at teste Qt/tray-adfaerd uden live Outlook eller Aula. Opret `tests/phase02/test_internet_notifications.py` som beviser GUI-log, tray-throttle og reset efter succes.
  </action>
  <verify>
    <automated>py -3 -m pytest tests/phase02/test_internet_notifications.py -q</automated>
  </verify>
  <done>Manglende internet giver ikke NameError; samme helper styrer GUI-log og tray-throttle i begge sync-knapper, og succes nulstiller den throttled state</done>
</task>

<task type="auto">
  <name>Task 2: Ret deleteEvent-failure path og caller-side successemantik</name>
  <files>aula/aula_calendar.py, main.pyw, tests/phase02/test_delete_failure_paths.py</files>
  <action>
Ret den konkrete BUG-04-typo i `AulaCalendar.deleteEvent()` saa failure-pathen ikke laengere kan kaste NameError. Stop ikke der: opdater callerne i `main.pyw`, saa `False` fra `deleteEvent()` og `updateEvent()` behandles som fejl og ikke som succes.

Bevar tydelig semantik:
- `True` = success
- `False` = kendt fejl
- `None` = timeout/ukendt fejl hvor den eksisterende kode allerede bruger det

Tilfoej `tests/phase02/test_delete_failure_paths.py`, som baade reproducerer delete-fejlstien og beviser at `__delete_aula_events()` og `__update_aula_events()` ikke logger succes paa `False`.
  </action>
  <verify>
    <automated>py -3 -m pytest tests/phase02/test_delete_failure_paths.py -q</automated>
  </verify>
  <done>`deleteEvent()` kaster ikke NameError ved failure, og callerne i `main.pyw` behandler ikke laengere `False` som succes</done>
</task>

<task type="auto">
  <name>Task 3: Indfoer no-.git version fallback og valider startup med kopieret runtime</name>
  <files>main.pyw, version.txt, tests/phase02/test_version_fallback.py</files>
  <action>
Erstat den nuvaerende direkte `git.Repo(...)`-afhaengighed i `MainWindow.setup_gui()` med en failure-tolerant helper der:

1. bruger git commit-dato, hvis `.git` findes
2. ellers laeser `version.txt`
3. ellers returnerer `None`, saa `program_version_label` skjules helt

Formatet i `version.txt` skal vaere samme menneskelaeselige datoformat som GUI'en allerede viser. Tilfoej `version.txt` i repoet som fallback-artefakt. Hold versionsvisning som valgfri metadata; lookup maa aldrig kunne crashe app-startup.

Verificer med `tests/phase02/test_version_fallback.py` og en app-naer no-`.git`-simulation fra en kopieret runtime-map pe frem for at omdoebe `.git` i arbejdsrepoet. Hvis package-near validering viser, at aktive spec-filer er noedvendige for at faa `version.txt` med, saa maa du opdatere den relevante spec-fil som en naerliggende fix; hvis det ikke er praktisk, dokumenter grænsen i summaryen i stedet for at udvide scope.
  </action>
  <verify>
    <automated>py -3 -m pytest tests/phase02/test_version_fallback.py -q</automated>
  </verify>
  <done>Appen kan starte uden `.git`; den viser fallback-dato fra `version.txt` naar filen findes, og skjuler labelen naar hverken git-metadata eller `version.txt` er tilgaengelig</done>
</task>

</tasks>

<verification>
- `py -3 -m pytest tests/phase02/test_internet_notifications.py -q`
- `py -3 -m pytest tests/phase02/test_delete_failure_paths.py -q`
- `py -3 -m pytest tests/phase02/test_version_fallback.py -q`
- `py -3 -m py_compile main.pyw aula\\aula_calendar.py`
- Kopier projektet til en midlertidig runtime-map uden `.git`, og bekraeft startup:
  1. med `version.txt` til stede -> label viser fallback-dato
  2. uden `version.txt` -> label skjules helt
- Hvis live Aula/Outlook ikke kan bruges, dokumenter graensen eksplicit i summaryen og behold de app-naere tests som hovedbevis
</verification>

<success_criteria>
1. BUG-03 er rettet som et centralt runtime-notifikationsflow, ikke kun som et enkelt navnereference-fix.
2. BUG-04 er lukket i baade callee og callers, saa failure ikke bliver rapporteret som succes.
3. BUG-05 er rettet med reel fallback-adfaerd, og startup uden `.git` er konkret verificeret.
</success_criteria>

<output>
Efter afslutning, opret `.planning/phases/02-fejlrettelser/02-02-SUMMARY.md` med:
- hvordan internetfejl-kanalerne blev implementeret og throttlet
- hvad der blev rettet i delete/update-failure semantics
- resultatet af no-`.git`-simulationen med og uden `version.txt`
- om spec-filer blev beroert eller bevidst ikke var noedvendige
</output>
